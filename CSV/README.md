Machine-readable coefficient tables

This folder contains machine-readable tables (`.csv`) with polynomial
coefficients of the moments μ_{2n}(V).

- Rows correspond to the moment index n.
- Columns correspond to coefficients of V^{2k}.
